# Core
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
python-dateutil>=2.8.0

# Dashboard
dash>=2.14.0
dash-bootstrap-components>=1.5.0
plotly>=5.18.0

# Forecasting
prophet>=1.1.5
pmdarima>=2.0.4
statsmodels>=0.14.0
xgboost>=2.0.0      # macOS: requires `brew install libomp` OR use Anaconda Python
scikit-learn>=1.3.0
torch>=2.0.0

# RAG
sentence-transformers>=2.5.0
faiss-cpu>=1.7.4
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
watchdog>=3.0.0

# ── macOS note ──────────────────────────────────────────────────────────────
# XGBoost requires OpenMP. If you see a libxgboost.dylib error:
#   Option A (recommended):  use /opt/anaconda3/bin/python  (already has libomp)
#   Option B:               brew install libomp

